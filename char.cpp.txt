complete code by yourself
#include<iostream>
#include<fstream>
using namespace std;
int main()
{
	char key_answers[11] = "TTTTTFFFFF";
	char answer[11] = "";
	
	cout << answer << endl;
	int count = 0;
	int j = 0;
	int reg[10] = {};
	int marks[10] = {};
	while (j < 3)
	{
		cout << "enter registration of student ";
		cin >> reg[j]; // 123
		cin.ignore();
		cout << "enter answers ";
		cin.getline(answer, 11);
		for (int i = 0; answer[i] != '\0'; i++)
		{
			if (key_answers[i] == answer[i])
			{
				marks[j] = count + 2;
			}
			else if (answer[i] == ' ')
			{
				marks[j] = count;
			}
			else
			{
				marks[j] = count - 1;
			}
		}
		j++;
	}
	cout << " reg no. arrays .. " << endl;
	for (int i = 0; i < 3; i++)
		cout << reg[i] << " ";
	for (int i = 0; i < 3; i++)
		cout << marks[i] << " ";
}
